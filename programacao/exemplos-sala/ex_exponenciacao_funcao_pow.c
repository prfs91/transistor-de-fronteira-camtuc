#include<stdio.h>
#include<math.h> //biblioteca math.h necessária para executar a função pow()

int main(void)
{
    
    // função pow(<base>,<potencia>), substitui os operadores (**) e (^)
    float f = pow(5,2);
    printf("%f\n", f);
    
}